
## Memory Update: 2026-04-14 05:22 UTC

- **Logo Assets:** Received official SVG logo (XML format) for Gaztechprime. Stored in memory for integration into the Header and Footer components.
- **Header Refactoring:** Completed the mobile-responsive Header with a functional `Sheet` (drawer) menu. Integrated accessibility features (`SheetTitle`, `SheetDescription`).
- **Footer Refactoring:** Fixed mobile layout alignment for contact information.
- **Catalog Features:** Implementation of the Grid/List view switcher in `app/catalog/[category]/page.tsx` is in progress. Transitioning to a hybrid Server/Client component structure to support stateful UI while maintaining SEO.
- **Operational Policy:** Strictly adhering to the `dev` branch workflow for all code changes to avoid direct `master` branch mutation.

## [2026-04-14 09:38] Deep Work Session: Gaztechprime Refactoring
- **Admin Panel Refactor**: 
  - Centralized dashboard connected to PostgreSQL (Prisma).
  - Split management lists (tables) and creation forms into separate routes (`/admin/products/new`, etc.).
  - Applied industrial dark/yellow design system.
- **Cart & Order System**:
  - Implemented `CartSidebar` with `shadcn/ui` Sheet.
  - Developed `createOrder` server action with Prisma persistence and Nodemailer integration.
  - Configured email notifications for `madnezz5@gmail.com` with full order details.
- **Architecture & Standards**:
  - Centralized environment variables in `lib/config.ts` (TS-safe access).
  - Standardized forms using `react-hook-form` + `zod` validation.
  - Unified types in `lib/types` based on Prisma models.
  - Enforced project conventions: `Next.js <Link>` for all routing, SVG storage in `/svg`, and Prettier formatting before push.
- **Infrastructure**:
  - Connected knowledge base (Obsidian Wiki) to GitHub repo `CyresSmith/zoitberg_soul`.
  - Maintained `dev` branch workflow for `CyresSmith/gaztehprime` (commits: `fcd3268`, `e215d01`, `0b38137`, `e8f0d0c`, `812a92b`, `59a3f16`, `524332a`).

## [2026-04-14 09:59 UTC] Durable update
- Gaztechprime progress completed today beyond earlier log:
  - Added global route loader with `nprogress` via `components/shared/global-loader.tsx`, integrated in `app/layout.tsx`, styled in `app/globals.css`.
  - Added admin detail page for leads/orders at `/admin/leads/[id]`.
  - Added portfolio create/edit admin pages and form (`/admin/portfolio/new`, `/admin/portfolio/[id]`, `PortfolioForm`).
  - Added category edit page `/admin/categories/[id]` and extended `CategoryForm` for edit mode.
  - Checkout now clears Zustand cart state after successful order.
  - Added global pointer-cursor UX requirement for interactive elements.
- Important user preference/architecture correction:
  - The knowledge base must follow Obsidian-native architecture.
  - Creating separate `skills/` and `projects/` directories inside the wiki/notes area likely breaks the intended Obsidian structure.
  - Rework those additions into properly structured `.md` notes, linked/indexed in the vault the way existing Obsidian docs expect, instead of forcing a foreign directory scheme.
- Process reminder:
  - When updating the wiki, actually align with `wiki/notes/AGENTS.md`, `Automation.md`, and existing Obsidian conventions, not just add content that looks organized.
  - User explicitly noticed architecture drift here and wants the vault cleaned up properly.

## [2026-04-14 10:35 UTC] Implementation update (Gaztechprime)
- **Catalog:** Grid/List view switcher for category page shipped. (git: `8e0ac28`)
- **Header:** Active-page indicator in header menu shipped.
- **Product page:** "Консультація інженера" CTA now navigates to `/contacts#consultation-form`.
- **Admin:** Listing tables refactored into reusable `AdminTable`, ensured edit + delete actions across Products/Categories/Portfolio/Leads. Added Leads dashboard widget + sidebar NEW badge. (git: `5c14cd5`)
- **Localization:** Lead statuses are displayed in Ukrainian in UI (Нова/Не оброблена/Оброблена) while DB keeps values `NEW|UNPROCESSED|PROCESSED`. (git: `c93ad9a`)
- **Knowledge base:** Updated Obsidian notes (`wiki/notes/*`) and created local commit `6cda43b`, but **push to GitHub is blocked** (SSH permission/deploy key issue).

## [2026-04-14 14:15 UTC] Gaztechprime Deep Work: Forms, Refactoring, & SEO
- **Refactoring:**
  - Separated `Lead` model into `Order` (checkout) and `Consultation` (contacts).
  - Built separate DB models, Prisma server actions (`createOrder`, `createConsultation`), and Admin Dashboard widgets.
  - Made lists and table names clickable in Admin panel for easy redirection to item detail pages.
- **Client Forms (Strict Validation):**
  - Fully enforced `react-hook-form` + `zod` for the user-facing contact form and checkout page.
  - Ensured Zod schemas and type definitions are strictly typed in `lib/schemas/index.ts` and `lib/types/index.ts`.
  - Stored `forms.md` to `MEMORY.md` and Wiki to mandate this structure moving forward.
- **SEO & PWA:**
  - Added strict Next.js error boundaries (`not-found.tsx`, `error.tsx`, `global-error.tsx`).
  - Added `/icon.svg` as favicon and configured `manifest.ts` (PWA).
  - Dynamically generating `sitemap.xml` mapping DB categories and products.
  - Implemented `generateMetadata` for dynamic Product and Category pages (`Title`, `Description`, `OpenGraph`).
  - Implemented **On-Demand Revalidation**: `revalidatePath('/sitemap.xml')` and `/catalog/...` mapped into all mutations (`createProduct`, `updateProduct`, etc) to avoid background refresh loops (`revalidate = 3600`) and ensure instant invalidation on save.
- All code pushed to `dev` branch on `CyresSmith/gaztehprime`.
