# Daily Log: 2026-04-15

## Work Done
- **Sidebar Fix:** Corrected admin layout for full-height sidebar and independent scroll area (`h-screen` + `overflow-hidden` patterns).
- **Image Upload System (V2):**
    - Created `uploadImage` service with support for Local and Cloudinary strategies.
    - Switched to using `CLOUDINARY_URL` for simplified Cloudinary configuration.
    - Implemented `/api/upload` API route.
    - Created `ImageUpload` component with **Drag-and-Drop** sorting using `@hello-pangea/dnd`.
    - Integrated image upload into Product, Category, and Portfolio forms.
- **Environment Management:** Centralized all variables into `lib/config.ts`.
    - Added `UPLOAD_STRATEGY` and `CLOUDINARY_URL`.
    - Added validation/warnings for missing keys.
- **Auth Fix:** Refactored `auth.ts` to fix NextAuth v5 "not callable" error and added `trustHost: true` for Vercel.
- **Settings Page:** Fully implemented the admin settings interface.

## Decisions
- **Senior Refactoring:** Cleaned up `process.env` calls to comply with project standards (`lib/config.ts` only).
- **Security:** Removed secret tokens from logs and history.

## Lessons Learned
- NextAuth v5 requires `trustHost: true` when deployed on Vercel with Next.js 16.
- Centralized config validation prevents silent failures in production.
