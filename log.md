# Log

## [2026-04-14] Повна інтеграція Кошика та Комунікацій.
- **Cart System (Zustand):**
  - Інстальовано `zustand` для керування станом кошика.
  - Реалізовано `useCart` хук з підтримкою `localStorage` (persist).
  - Оновлено `CartSidebar.tsx`: тепер він повністю динамічний (зміна кількості, видалення, підсумок).
- **Catalog & Product Page:**
  - Створено компонент `ProductActions.tsx` для картки товару.
  - Додано функціонал "Додати в кошик" та "Швидка консультація" з інтеграцією `createOrder`.
  - Додано візуальний фідбек через `toast` сповіщення.
- **Contacts & Forms Refactor:**
  - Всі контакти (телефони, email) переведено на `Next Link` для миттєвої реакції.
  - Поля форм отримали атрибут `required`, кнопки переходять у стан `loading` під час відправки.
- **Git:** Коміт `e5b1697` у `dev`.

## [2026-04-14] Рефакторинг розділів "Заявки" та "Наші роботи".
- Побудовано табличне керування Leads та PortfolioProject.
- Git Hash: `faedf13`
